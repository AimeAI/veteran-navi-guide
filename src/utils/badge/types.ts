
import { VeteranBadge, BadgeType } from "@/components/ui/veteran-badge";

// Badge definitions and types can be placed here if needed
// This file is mostly for re-exports to maintain the current API
export type { VeteranBadge, BadgeType };
